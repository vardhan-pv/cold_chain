# Original uploaded project

These source files and SQLite database snapshots are retained unchanged for comparison and historical evidence. The running code lives in the main project folders. Do not start a second backend from this archive.

First startup imports the database rows into dedicated historical tables without replaying any old command. Use the dashboard's Original project records view. Snapshot hashes and counts are in manifest.json. The old virtual environment and generated bytecode are deliberately omitted.
